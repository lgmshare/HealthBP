<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="com.obody.health.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthBP"
        tools:targetApi="31">

        <activity
            android:name=".feature.WelcomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.MainActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.ArticleActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.PrivacyActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.AddActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".feature.RecordActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".feature.RecordListActivity"
            android:screenOrientation="portrait" />
    </application>
</manifest>